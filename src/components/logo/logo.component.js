import React from 'react';

import logo from '../../../public/img/logo.png';

const Logo = () => (
    <div className="logo">
        <span className="logo__text">Logo</span>
    </div>
);

export default Logo;